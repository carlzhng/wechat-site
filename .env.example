# Copy this file to .env and set your own password before going live.
ADMIN_PASSWORD=your_password_here
SESSION_SECRET=random-long-string-here
PORT=3000

# Vercel only: created automatically when you add a Blob store in the Vercel project.
# Local dev does not need this — catalog and uploads use data/ and uploads/ instead.
# BLOB_READ_WRITE_TOKEN=
